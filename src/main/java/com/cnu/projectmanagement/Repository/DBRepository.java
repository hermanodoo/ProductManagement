package com.cnu.projectmanagement.Repository;//package com.cnu.productmanagement.Repository;
/*


@Repository
public class DBRepository extends ProductRepository {

    @PersistenceContext
    private EntityManager entityManager;


    public ProductEntity save(ProductEntity product) {
        entityManager.persist(product);
        return product;
    }


    public Optional<ProductEntity> findById(Long productId) {
        return Optional.ofNullable(entityManager.find(ProductEntity.class, productId));
    }


    public List<ProductEntity> findAll() {

    }


    public ProductEntity update(ProductEntity product) {
        return entityManager.merge(product);
    }


    public void deleteById(Long id) {
        Optional<ProductEntity> product = findById(id);
        if (product.isPresent()) {
            entityManager.remove(product);
        }
    }


    public void delete(ProductEntity product) {
        entityManager.remove(product);
    }


    public void flush() {
        entityManager.flush();
    }
}
*/
